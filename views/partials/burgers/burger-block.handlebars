<li>
	{{#if devoured}}
	<h3>{{Burger.burger_name}}</h3>
	<button class="change-devoured btn btn-light" data-id="{{Burger.id}}"
		data-newdevoured="{{devoured}}">DEVOR!</button></button>
	{{else}}
	<h3>
		{{Burger.burger_name}} was delicious!
	</h3>
	{{/if}}
</li>